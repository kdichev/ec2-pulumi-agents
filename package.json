{
  "name": "pulumi-ec2-agents",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build": "yarn workspace app build"
  },
  "devDependencies": {
    "@types/node": "^10.0.0"
  },
  "dependencies": {
    "@pulumi/pulumi": "^2.0.0",
    "@pulumi/aws": "^2.0.0",
    "@pulumi/awsx": "^0.20.0"
  }
}
